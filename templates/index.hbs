<!DOCTYPE html>
<html>
    <body>
        <ul>
            {{#each project}}
                <li>{{@key}}, reference: {{{this.repodocReference}}}, projectType: {{this.projectType}}, description: {{ this.repodocDescription }}, root: {{this.root}}, sourceRoot: {{this.sourceRoot}}</li>
            {{/each}}

        </ul>
    </body>
</html>